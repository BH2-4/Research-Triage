# 假设提出与验证

## 核心原则

所有 Plan 中的建议必须以假设形式呈现，格式如下：

> **假设**：如果按 [X] 路线走，
> **预期结果**：[Y]，
> **验证方法**：[Z]

## 规则

- 不可验证的假设 = 无效假设。每条假设必须带验证方法。
- 假设的验证方法必须匹配用户的工具能力（画像中的 toolAbility 字段）
- 如果用户没有验证该假设的工具 → 给出替代表述或降级验证方法
- Plan 中每条 actionStep 都必须能追溯到至少一个假设
