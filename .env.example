# ─────────────────────────────────────────────────────────
# AI Provider — any OpenAI-compatible API
# ─────────────────────────────────────────────────────────
# Change these three variables to switch providers. No code edits needed.

AI_BASE_URL=https://api.deepseek.com/v1
AI_API_KEY=sk-your-key-here
AI_MODEL=deepseek-v4-flash
AI_REQUEST_TIMEOUT_MS=45000

# Required in production for HttpOnly session ownership cookies.
SESSION_COOKIE_SECRET=replace-with-a-random-32-byte-secret

# ─── Examples ────────────────────────────────────────────
#
# DeepSeek:
#   AI_BASE_URL=https://api.deepseek.com/v1
#   AI_MODEL=deepseek-v4-flash  (or deepseek-v4-pro)
#
# OpenAI:
#   AI_BASE_URL=https://api.openai.com/v1
#   AI_MODEL=gpt-4o-mini
#
# Moonshot / Kimi:
#   AI_BASE_URL=https://api.moonshot.cn/v1
#   AI_MODEL=moonshot-v1-8k
#
# Zhipu GLM:
#   AI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
#   AI_MODEL=glm-4-flash
#
# OpenRouter (multi-provider gateway):
#   AI_BASE_URL=https://openrouter.ai/api/v1
#   AI_MODEL=anthropic/claude-3.5-sonnet
#
# Local ollama:
#   AI_BASE_URL=http://localhost:11434/v1
#   AI_API_KEY=ollama
#   AI_MODEL=qwen2.5:7b

# Set true only when a trusted proxy strips and supplies these headers.
# Leave false when the Node.js origin is directly reachable.
TRUST_PROXY_HEADERS=false
